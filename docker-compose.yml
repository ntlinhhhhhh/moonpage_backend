services:
  diary-api:
    build:
      context: .
      dockerfile: Dockerfile 
    ports:
      - "8000:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production

      # google cloud & firebase
      - GoogleCloud__ProjectId=moodyfy-3f2dd
      - GoogleCloud__ClientId=511455154390-m8jmplb158akjj874288pipkt3emset0.apps.googleusercontent.com
      - GoogleCloud__StorageBucket=moodyfy-3f2dd.firebasestorage.app

      - GoogleCloud__ServiceAccountPath=/app/firebase-key.json
      - Firebase__ProjectId=moodyfy-3f2dd

      # jwt settings
      - JwtSettings__Secret=NNTExNDU1MTU0MzkwLW04am1wbGIxNThha2pqODc0Mjg4cGlwa3QzZW1zZXQwLmFwcHMuZ29vZ2xldXNlcm>
      - JwtSettings__Issuer=DiaryApp
      - JwtSettings__Audience=DiaryApp
      - JwtSettings__ExpiryMinutes=43200

      # email settings
      - EmailSettings__Email=diaryapp23@gmail.com
      - EmailSettings__Password=uyozvhaobagwzocc
      - EmailSettings__Host=smtp.gmail.com
      - EmailSettings__Port=587

      # redis & rabbitmq
      - Redis__ConnectionString=diary-redis:6379
   